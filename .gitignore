# CMake — always reconfigure on a new machine / in Docker (paths differ from host cache).
/build/
/build-test/
cmake-build-*/
/CMakeFiles/
/Makefile
/cmake_install.cmake
/CMakeCache.txt
/_deps/
/practice/

# Runtime artifacts
oms_audit.log
*.log

# Compiled binaries in project root
/oms_test
/trading_bot

# Secrets — never commit. See .env.example for the template.
.env
.env.local
.env.*.local
